apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ip-sentry.fullname" . }}-watcher-script
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ip-sentry.labels" . | nindent 4 }}
data:
  watcher.py: |
{{ .Files.Get "files/watcher.py" | indent 4 }}
